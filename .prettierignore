dist
node_modules
pnpm-lock.yaml
package-lock.json

# separate Vercel project with its own tooling, see email-api/README
email-api

# CMS-authored content — Decap commits these directly, they shouldn't be
# held to the same formatting rules as hand-written code.
content/blog/**