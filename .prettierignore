dist
node_modules
pnpm-lock.yaml
package-lock.json

# CMS-authored content — Decap commits these directly, they shouldn't be
# held to the same formatting rules as hand-written code.
content/blog/**